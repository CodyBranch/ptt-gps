# Mapbox publishable token for the console's maps.
# Copy this file to admin-ui/.env and fill in the token from the Mapbox account
# (Account -> Tokens). It is a "pk." token: public by design and safe to ship in
# the built JS, but kept out of source so the repository holds no credentials.
VITE_MAPBOX_TOKEN=
