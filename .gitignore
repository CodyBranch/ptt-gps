# Legacy system — contains embedded credentials and race logs; kept locally as reference only
legacy/

node_modules/
dist/
*.tsbuildinfo

# Runtime data (SQLite DB, raw frame logs)
data/

# Secrets
.env
.env.*
!.env.example
*serviceaccount*.json
*firebase-adminsdk*.json
secrets/
