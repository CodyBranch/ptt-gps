# Legacy system — contains embedded credentials and race logs; kept locally as reference only
legacy/

node_modules/
dist/
*.tsbuildinfo

# Runtime data (SQLite DB, raw frame logs)
data/

# Secrets
.env
.env.*
!.env.example
*serviceaccount*.json
*firebase-adminsdk*.json
secrets/

# The standalone manual HTML is an 11 MB intermediate on the way to the PDF —
# the in-app Help page shows the same content. Regenerate with:
#   npm run manual
admin-ui/public/docs/primetime-gps-manual.html

# WinSW, fetched by deploy/install-service.ps1. A service host binary
# belongs on the machine, not in the repository.
deploy/ptt-gps.exe
deploy/*.exe
logs/

# Rendered from ptt-gps.xml.template by install-service.ps1: the path to node
# is a property of the machine, not of the project.
deploy/ptt-gps.xml
